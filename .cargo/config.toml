# Default platform-metadata JDK for build scripts: the jlink'd root the
# Makefile also builds. `relative = true` anchors it to the workspace root.
# A real environment variable (Makefile, CI) takes precedence over this value.
[env]
MADURA_JAVA_HOME = { value = "target/jdkroot", relative = true }

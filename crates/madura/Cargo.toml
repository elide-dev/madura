cargo-features = ["trim-paths"]

[package]
name = "madura"
version = "0.1.0"
edition = "2024"

[dependencies]
madura_javac = { path = "../madura_javac" }
mimalloc = { workspace = true, features = ["v2"] }
